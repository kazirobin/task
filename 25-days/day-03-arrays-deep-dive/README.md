### Day 3 – Arrays Deep Dive
**Concepts**
- map
- filter
- reduce
- forEach
**Practice**
Given array of expenses:
- Calculate total
- Filter expenses > 1000
- Convert currency
**Mini Task**
Build Expense Summary Script
**Commit**
feat: built an expense summary script
Step-by-step explanation:
1. Use `map` to transform each array element (e.g., convert currencies).
2. Use `filter` to create a new array with elements that pass a test (e.g., >1000).
3. Use `reduce` to accumulate a value (e.g., sum total).
4. Use `forEach` to iterate over elements without returning a new array.
5. For the expenses array, apply these methods to summarize data.
6. Build a script that outputs the summary.
7. Commit your code.